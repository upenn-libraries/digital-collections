<%# Copied template from Blacklight 9.0.0.beta1 to combine constraints/sort into one div %>

<div>
  <%= render helpers
    .blacklight_config
    .view_config(helpers.document_index_view_type)
    .constraints_component
    .new(search_state: helpers.search_state) %>
  <%= render "sort_and_per_page" %>
</div>
